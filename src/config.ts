export const IMAGE_LIMITS = {
  maxFileSize: 20 * 1024 * 1024,
  maxWidth: 10_000,
  maxHeight: 10_000,
  maxPixels: 40_000_000,
} as const;

export const MODEL_ID = 'Xenova/modnet';
export const CONTACT_EMAIL = 'hello@clearcut.tools';
